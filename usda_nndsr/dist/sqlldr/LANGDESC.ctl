OPTIONS (DIRECT=TRUE, PARALLEL=TRUE)
LOAD DATA
    INFILE './data.processed/LANGDESC.txt.trimmed'
    APPEND
    INTO TABLE LANGDESC
    FIELDS TERMINATED BY '^'
    OPTIONALLY ENCLOSED BY '~'
    TRAILING NULLCOLS
    (Factor_Code CHAR, Description CHAR)