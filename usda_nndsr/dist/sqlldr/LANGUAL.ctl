OPTIONS (DIRECT=TRUE, PARALLEL=TRUE)
LOAD DATA
    INFILE './data.processed/LANGUAL.txt.trimmed'
    APPEND
    INTO TABLE LANGUAL
    FIELDS TERMINATED BY '^'
    OPTIONALLY ENCLOSED BY '~'
    TRAILING NULLCOLS
    (NDB_No CHAR, Factor_Code CHAR)