OPTIONS (DIRECT=TRUE, PARALLEL=TRUE)
LOAD DATA
    INFILE './data.processed/SRC_CD.txt.trimmed'
    APPEND
    INTO TABLE SRC_CD
    FIELDS TERMINATED BY '^'
    OPTIONALLY ENCLOSED BY '~'
    TRAILING NULLCOLS
    (Src_Cd CHAR, SrcCd_Desc CHAR)