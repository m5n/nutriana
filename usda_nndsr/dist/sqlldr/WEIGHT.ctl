OPTIONS (DIRECT=TRUE, PARALLEL=TRUE)
LOAD DATA
    INFILE './data.processed/WEIGHT.txt.trimmed'
    APPEND
    INTO TABLE WEIGHT
    FIELDS TERMINATED BY '^'
    OPTIONALLY ENCLOSED BY '~'
    TRAILING NULLCOLS
    (NDB_No CHAR, Seq CHAR, Amount DECIMAL EXTERNAL, Msre_Desc CHAR, Gm_Wgt DECIMAL EXTERNAL, Num_Data_Pts INTEGER EXTERNAL, Std_Dev DECIMAL EXTERNAL)