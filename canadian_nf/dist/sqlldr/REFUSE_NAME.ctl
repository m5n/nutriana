OPTIONS (DIRECT=TRUE, PARALLEL=TRUE, SKIP=1)
LOAD DATA
    INFILE './data.processed/REFUSE_NAME.csv.trimmed'
    APPEND
    INTO TABLE REFUSE_NAME
    FIELDS TERMINATED BY ','
    OPTIONALLY ENCLOSED BY '"'
    TRAILING NULLCOLS
    (RefuseID INTEGER EXTERNAL, RefuseName CHAR, RefuseNameF CHAR)