OPTIONS (DIRECT=TRUE, PARALLEL=TRUE, SKIP=1)
LOAD DATA
    INFILE './data.processed/YIELD_NAME.csv.trimmed'
    APPEND
    INTO TABLE YIELD_NAME
    FIELDS TERMINATED BY ','
    OPTIONALLY ENCLOSED BY '"'
    TRAILING NULLCOLS
    (YieldID INTEGER EXTERNAL, YieldName CHAR, YieldNameF CHAR)